{
  "data": {
    "recon_errors": { "file": "data/recon_errors.parquet" }
  },
  "params": { "selRecon": { "select": "single" } },
  "vconcat": [
    { "input": "menu", "from": "recon_errors", "column": "run_id", "label": "Run", "as": "$selRecon" },
    {
      "plot": [
        {
          "mark": "rectY",
          "data": { "from": "recon_errors", "filterBy": "$selRecon" },
          "x": { "bin": "error" },
          "y": { "count": true },
          "fill": { "sql": "CASE WHEN label = 0 THEN 'normal' ELSE 'attack' END" },
          "insetLeft": 0.5,
          "insetRight": 0.5
        },
        { "legend": "color" }
      ],
      "width": 650, "height": 320,
      "colorDomain": ["normal", "attack"],
      "colorRange": ["#3fb950", "#f85149"],
      "xLabel": "Reconstruction Error",
      "yLabel": "Count"
    }
  ]
}
