# Small-scale VGAE architecture.
# Compose: --config stages/autoencoder.yaml --config models/vgae/small.yaml
model:
  init_args:
    hidden_dims: [80, 40, 16]
    latent_dim: 16
    heads: 1
    embedding_dim: 4
    dropout: 0.1
    proj_dim: 32
