# KD auxiliary defaults for VGAE student.
# Appended to config chain when recipe has auxiliaries.
# teacher_scale, model_path resolved at runtime by prepare_kd().
model:
  init_args:
    auxiliaries:
      - type: kd
        alpha: 0.7
        vgae_latent_weight: 0.5
        vgae_recon_weight: 0.5
