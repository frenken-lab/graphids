# Small-scale GAT architecture.
# Compose: --config stages/normal.yaml --config models/gat/small.yaml
model:
  init_args:
    hidden: 24
    layers: 2
    heads: 4
    embedding_dim: 8
    fc_layers: 2
    dropout: 0.1
    proj_dim: 32
