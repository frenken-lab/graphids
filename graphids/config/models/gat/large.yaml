# Large-scale GAT architecture (sweep-optimized).
# Compose: --config stages/curriculum.yaml --config models/gat/large.yaml
model:
  init_args:
    hidden: 64
    layers: 3
    heads: 4
    embedding_dim: 8
    fc_layers: 4
    dropout: 0.11
    proj_dim: 48
    scale: large
