# Large-scale DGI architecture (shares VGAE topology).
# Compose: --config stages/autoencoder.yaml --config models/dgi/large.yaml
model:
  init_args:
    hidden_dims: [480, 240, 64]
    latent_dim: 64
    heads: 4
    embedding_dim: 32
    dropout: 0.15
    proj_dim: 48
    scale: large
