Jsonnet handles:                    Python handles:
─────────────────────               ──────────────────────────
model architecture                  SLURM profile injection
hyperparameters                     dataset path resolution
training plan composition           schema version migration
ablation deltas                     CLI override parsing
stage overrides                     Pydantic validation
                                    git provenance capture
                                    sweep state tracking
                                    SLURM script rendering
