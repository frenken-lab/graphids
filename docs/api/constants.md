# Constants

Project-wide constants and the typed view over
``configs/matrix/axes.json``. Layer 1 is torchless and imports freely
at package load; Layer 2 aliases read from
[settings](settings.md) via ``get_settings()``.

## `graphids.config.constants`

::: graphids.config.constants
