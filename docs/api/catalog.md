# Catalog

Dataset catalog helpers and filesystem path primitives. Provides
`data_dir` / `cache_dir` (raw + tensor cache paths under `LAKE_ROOT`),
`load_catalog` (flat view of `dataset_registry.json`), and
`dataset_names` (public entries only).

## `graphids.config.catalog`

::: graphids.config.catalog
